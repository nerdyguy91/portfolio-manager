{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["trigger", "why_it_matters", "suggested_actions", "confidence"],
  "additionalProperties": false,
  "properties": {
    "trigger": {
      "type": "string",
      "description": "The specific data condition that fired this alert."
    },
    "why_it_matters": {
      "type": "string",
      "description": "Plain-language explanation of the significance for a dividend investor."
    },
    "suggested_actions": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Ordered list of recommended steps the investor should consider."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Model confidence in this explanation (0–1). Below 0.75 triggers human review."
    }
  }
}
